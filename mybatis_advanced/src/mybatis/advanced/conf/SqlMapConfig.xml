<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>

	<properties resource="mybatis/advanced/conf/db.properties" />

	<typeAliases>
		<typeAlias alias="Blog" type="mybatis.advanced.model.Blog" />
		<typeAlias alias="Author" type="mybatis.advanced.model.Author" />
		<typeAlias alias="Comm" type="mybatis.advanced.model.Comm" />
		<typeAlias alias="CommSearcher" type="mybatis.advanced.model.CommSearcher" />
	</typeAliases>
	
	<environments default="development">
		<environment id="development">
			<transactionManager type="JDBC" />
			<dataSource type="POOLED">
				<property name="driver" value="${driver}" />
				<property name="url" value="${url}" />
				<property name="username" value="${username}" />
				<property name="password" value="${password}" />
			</dataSource>
		</environment>
	</environments>
	
	<mappers>
		<mapper resource="mybatis/advanced/conf/Blog.xml" />
	</mappers>
	
</configuration>